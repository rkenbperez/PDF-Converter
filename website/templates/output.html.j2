{% extends "base.html.j2" %}
{% block content %}
<div class="min-h-screen flex items-center justify-center bg-gray-900 p-8">
    <div class="bg-white p-8 rounded-lg shadow-lg text-center max-w-md w-full">
        <!-- Success Icon -->
        <div class="mb-6">
            <div class="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mx-auto">
                <svg class="w-8 h-8 text-green-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                </svg>
            </div>
        </div>
        
        <!-- Success Message -->
        <h1 class="text-2xl font-bold text-gray-800 mb-2">PDF Created Successfully! 🎉</h1>
        <p class="text-gray-600 mb-8">Your images have been converted to PDF and are ready to download.</p>
        
        <!-- Action Buttons -->
        <div class="space-y-4">
            <!-- Download Button -->
            <a href="{{ url_for('download_pdf', pdf_name=output_pdf) }}" 
            class="block w-full bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg transition-colors duration-300 text-center">
                📥 Download PDF
            </a>
            
            <!-- Convert Another Button -->
            <a href="{{ url_for('home') }}" 
            class="block w-full bg-gray-600 hover:bg-gray-700 text-white font-bold py-3 px-6 rounded-lg transition-colors duration-300 text-center">
                🔄 Convert Another File
            </a>
        </div>
        
        <!-- File Info -->
        <div class="mt-6 p-4 bg-gray-50 rounded-lg">
            <p class="text-sm text-gray-600">
                <strong>File:</strong> {{ output_pdf }}<br>
                <strong>Format:</strong> PDF Document
            </p>
        </div>
        
        <!-- Auto-download Script -->
        <script>
            // Uncomment the line below if you want automatic download
            // window.location.href = "{{ url_for('download_pdf', pdf_name=output_pdf) }}";
        </script>
    </div>
</div>
{% endblock %}